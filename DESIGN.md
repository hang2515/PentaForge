# 英雄联盟高光剪辑工具 — 需求分析与阶段规划

> 最后更新: 2026-05-18

## 一、项目概述

半自动/自动《英雄联盟》高光剪辑工具，输入游戏录屏视频，自动识别多杀精彩片段，完成音频替换、片段拼接、转场特效，最终导出成品视频。

**核心流水线：** 录屏输入 → 高光检测 → 片段裁剪 → 音频处理 → 拼接转场 → 成片导出

---

## 二、阶段规划

```
Phase 1 ─── 稳定自动化剪辑流水线（半自动）
  │  用户手动提供时间戳，系统完成裁剪、混音、拼接、导出
  │  目标：跑通端到端流程，产出可用的成品视频
  │
Phase 2 ─── 智能五杀识别（自动检测）
  │  从视频中自动识别多杀事件，无需手动标记时间戳
  │  目标：丢入录屏即可自动找高光
  │
Phase 3+ ── 高级视觉特效
    镜头聚焦、动态缩放、慢动作、蒙版叠加等
```

---

## 三、Phase 1 — 稳定自动化剪辑流水线

### 3.1 目标

用户提供一段游戏录屏 + 一份时间戳配置，工具自动完成所有后期处理，输出可直接发布的成品视频。

### 3.2 输入

| 输入项 | 说明 | 适用模式 |
|--------|------|----------|
| 游戏录屏 | 单个或多个视频文件（mp4/mov/avi） | 通用 |
| 时间戳配置 | JSON/YAML，标记每个高光片段的起止时间 | 通用 |
| BGM 音频 | 背景音乐文件（mp3/wav），可选多段 | 仅模式 2 |
| 击杀音效 | 击杀提示音效文件（mp3/wav），对应不同多杀等级 | 仅模式 2 |
| 导出配置 | 分辨率、码率、帧率、输出格式 | 通用 |

### 3.3 时间戳配置格式

通过 `audio_enabled` 字段切换音频模式。其他流程（裁剪、拼接、转场、导出）完全一致。

#### 模式 1：保留原声（简洁配置）

```yaml
# highlight.yaml
source: "game_recording_01.mp4"
output: "highlight_01.mp4"
audio_enabled: false        # 关闭音频替换，保留原视频声音

transitions:
  type: "fade"
  duration: 0.3

export:
  resolution: "1920x1080"
  fps: 60
  codec: "h264"
  bitrate: "12M"

clips:
  - start: "00:03:15.200"
    end:   "00:03:27.500"
    label: "第一波三杀"
    transition_after:
      type: "fade"
      duration: 0.3
  - start: "00:12:40.000"
    end:   "00:13:02.000"
    label: "五杀团战"
```

#### 模式 2：音频替换（完整配置）

```yaml
# highlight.yaml
source: "game_recording_01.mp4"
output: "highlight_01.mp4"
audio_enabled: true          # 开启音频替换，剥离原声
bgm: "bgm.mp3"
bgm_volume: 0.3
sfx:
  double_kill: "assets/sfx/double_kill.mp3"
  triple_kill: "assets/sfx/triple_kill.mp3"
  quadra_kill: "assets/sfx/quadra_kill.mp3"
  penta_kill: "assets/sfx/penta_kill.mp3"
sfx_volume: 0.8

transitions:
  type: "fade"
  duration: 0.3

export:
  resolution: "1920x1080"
  fps: 60
  codec: "h264"
  bitrate: "12M"

clips:
  - start: "00:03:15.200"
    end:   "00:03:27.500"
    kill_type: "triple_kill"
    label: "第一波三杀"
    transition_after:
      type: "fade"
      duration: 0.3
  - start: "00:12:40.000"
    end:   "00:13:02.000"
    kill_type: "penta_kill"
    label: "五杀团战"
```

### 3.4 模块划分

```
┌──────────────────────────────────────────────────┐
│                  CLI / 配置解析                     │
└──────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  片段裁剪     │ │  音频处理     │ │  转场拼接     │
│  (FFmpeg)    │ │  (FFmpeg)    │ │  (FFmpeg)    │
└──────────────┘ └──────────────┘ └──────────────┘
        │               │               │
        └───────────────┼───────────────┘
                        ▼
┌──────────────────────────────────────────────────┐
│                  导出成品视频                       │
└──────────────────────────────────────────────────┘
```

#### 模块 A：配置解析器
- 读取并校验 YAML/JSON 配置文件
- 解析时间戳（支持多种格式）
- 校验依赖文件存在性

#### 模块 B：片段裁剪器
- 基于 FFmpeg 精确裁剪指定时间段
- 保留原画质和帧率
- 模式 1：保留原音频；模式 2：去除原音频（由 `audio_enabled` 控制）

#### 模块 C：音频处理器（仅模式 2）
- 剥离原始游戏音频
- 混入 BGM（循环/截取适配片段长度）
- 在击杀时间点叠加击杀音效
- 音量归一化处理

#### 模块 D：转场拼接器
- 将多个片段按顺序拼接
- 片段间加入过渡转场（fade in/out, crossfade）
- 支持全局默认转场，也支持在每个 clip 上用 `transition_after` 指定到下一段的转场
- 可选片头/片尾

#### 模块 E：导出器
- 统一编码输出
- 支持 H.264/H.265 编码
- 多分辨率输出

### 3.5 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 核心引擎 | **FFmpeg** | 视频处理事实标准，支持所有操作 |
| 编排语言 | **Python 3.11+** | 生态丰富，FFmpeg 绑定成熟 |
| FFmpeg 绑定 | **ffmpeg-python** | 链式 API，比 subprocess 更可控 |
| 配置格式 | **YAML** (PyYAML) | 可读性好，支持注释 |
| 音频处理 | FFmpeg audio filters | 混音、音量、淡入淡出均内置 |
| 包管理 | pip + venv | 使用 `.venv` 和 `requirements*.txt`，当前不使用 Docker/Poetry |

### 3.6 Phase 1 任务拆解

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| 1.1 | 项目脚手架：目录结构、依赖管理、CLI 入口 | 可运行的空壳 | 已完成 |
| 1.2 | 配置解析模块：YAML/JSON 读取、校验、默认值、相对路径解析 | 配置 → 内部数据结构 | 已完成 |
| 1.3 | 片段裁剪模块：FFmpeg seek + trim | 从源视频切出片段，支持保留原声/去原声 | 已完成 |
| 1.4 | 音频处理模块：去原音、混 BGM、叠 SFX（仅模式 2） | 片段带新音轨 | 已完成 |
| 1.5 | 转场拼接模块：concat + transition filters | 多片段合并为单一视频，支持全局默认转场和逐段 `transition_after` | 已完成 |
| 1.6 | 导出模块：编码参数控制、进度反馈 | 最终 mp4 文件，支持分辨率/FPS/编码器/码率/preset | 已完成 |
| 1.7 | 集成测试 + 示例配置 | 端到端可跑通 | 已完成 |
| 1.8 | Web 配置界面：双音频模式、片段编辑、逐段转场编辑 | 表单/YAML 双入口配置 | 已完成 |
| 1.9 | 本地环境说明：非 Docker 安装、启动、测试、OCR 可选依赖 | 新电脑可按文档恢复运行环境 | 已完成 |
| 1.10 | 质量验收：PSNR、音画同步、黑帧、处理速度 | 可量化验收报告 | 待完成 |

### 3.7 双模式说明

两种模式的差异仅限于音频处理，其余流程（裁剪、拼接、转场、导出）完全一致，由 `audio_enabled` 字段控制。

| | 模式 1：保留原声 | 模式 2：音频替换 |
|---|---|---|
| `audio_enabled` | `false` | `true` |
| 视频原声 | 保留，直接拼接 | 剥离，丢弃 |
| BGM 背景音乐 | 不使用 | 混入，音量可调 |
| SFX 击杀音效 | 不使用 | 在击杀时间点叠加 |
| 配置复杂度 | 极简（仅需 clips） | 需配置 bgm/sfx 路径 |
| 适用场景 | 快速出片、保留解说 | 高光集锦、配乐剪辑 |
| 后期扩展 | AI 人声分离（分离解说与游戏音效） | — |

> **后期计划：** 模式 1 下可接入 AI 人声分离模型（如 spleeter / demucs），将原视频音轨中的人声（解说）与游戏音效分离，实现"保留击杀播报、去除解说语音"的精细化控制。

### 3.8 Phase 1 成功标准

- [x] 给定一段录屏 + 一份 YAML 配置，一键产出成品视频
- [ ] 视频画质无明显损失（与源文件对比 PSNR > 40dB）
- [ ] 音画同步误差 < 50ms
- [ ] 转场流畅无黑帧
- [ ] 处理速度不低于 0.5x 实时（即 10 分钟素材 < 20 分钟处理）

### 3.9 Phase 1 进度记录

| 日期 | 进度 |
|------|------|
| 2026-05-18 | 完成 Python `.venv` 依赖安装与测试依赖拆分，新增 `requirements-dev.txt`。 |
| 2026-05-18 | 修复双音频模式：模式 1 保留原声，模式 2 替换音频并校验 BGM/SFX。 |
| 2026-05-18 | 修复导出配置链路：分辨率、FPS、编码器、码率、preset 已接入最终导出。 |
| 2026-05-18 | 配置解析支持 YAML/JSON，相对路径按配置文件所在目录解析。 |
| 2026-05-18 | Web 界面显式显示“模式 1：保留原声 / 模式 2：替换音频”。 |
| 2026-05-18 | 实现逐段转场：每个 clip 可通过 `transition_after` 指定到下一段的转场。 |
| 2026-05-18 | 删除 Docker 方案，改为 `.venv` + Node.js 的本地运行方案，并新增 `LOCAL_SETUP.md`。 |
| 2026-05-18 | 后端测试通过 `51 passed`，前端 `npm run build` 通过。 |

---

## 四、Phase 2 — 智能五杀识别

### 4.1 目标

无需手动标记时间戳，工具自动从游戏录屏中检测多杀事件并定位时间窗口。

自动识别是辅助能力，不应替代手动标注。Phase 2 必须保留 Phase 1 的手动时间戳编辑能力：当 OCR/模板/音频识别漏检、误检或边界不准时，用户仍然可以手动新增片段、删除误检片段、调整起止时间和转场配置。

### 4.2 检测策略

英雄联盟的多杀检测有多种信号可用，建议**多信号融合**提高准确率：

```
策略 A: 击杀播报 OCR     ──┐
策略 B: 屏幕中央横幅识别   ──┼── 加权融合 ──→ 高光时间戳
策略 C: 播报员语音识别     ──┤
策略 D: 击杀日志解析       ──┘  (可选，需要 Riot API)
```

#### 策略 A：击杀播报 OCR（主策略）

- **原理**：游戏画面右侧/中央会显示击杀信息，OCR 提取文字
- **关注文本**：`双杀` `三杀` `四杀` `五杀` / `Double Kill` `Triple Kill` `Quadra Kill` `Penta Kill`
- **工具**：PaddleOCR（当前优先方案）或 Tesseract OCR（中文需额外训练数据）
- **优化**：限定 ROI（region of interest）减少 OCR 计算量；帧采样间隔 0.5-1s

#### 策略 B：中央横幅模板匹配

- **原理**：多杀时屏幕中央会出现大号文字横幅，可用模板匹配
- **方式**：预先截取各多杀横幅图片作为模板，在视频帧中做特征匹配（ORB/SIFT）
- **优点**：比 OCR 更快、更准，但需要每种语言/分辨率各准备模板

#### 策略 C：播报员语音识别

- **原理**：多杀时游戏会播放特定语音（"Triple Kill!"），识别音频
- **方式**：音频指纹匹配（预先录制各多杀播报音频），在视频音轨中搜索
- **工具**：audfprint / 自建 MFCC 特征库
- **挑战**：BGM 干扰、不同语音包差异

### 4.3 片段边界算法

检测到击杀时间点后，需要自动确定片段起止边界：

```
起始点 = 击杀时间点 - 回溯窗口（默认 8s）
结束点 = 击杀时间点 + 前瞻窗口（默认 4s）
```

- 回溯窗口要包含击杀前的团战过程
- 前瞻窗口留出收尾空间
- 多个击杀点时间接近时自动合并为一个片段

### 4.4 Phase 2 任务拆解

| # | 任务 | 产出 | 状态 |
|---|------|------|------|
| 2.1 | 视频帧采样与 ROI 提取 | 定时抽取关键帧，裁出击杀播报区域 | 已完成基础版 |
| 2.2 | OCR 识别流水线（PaddleOCR） | 帧 → 文本列表 | 已完成基础版（PaddleOCR 已接入并自检通过） |
| 2.3 | 多杀事件解析与时间戳提取 | 文本匹配 → 事件列表 | 已完成基础版 |
| 2.4 | 片段边界自动计算（回溯/前瞻窗口） | 事件 → clip 时间窗口 | 已完成基础版 |
| 2.5 | 与 Phase 1 流水线集成 | 自动候选片段可追加到手动片段列表，再由既有流水线导出 | 已完成基础版 |
| 2.6 | 准确率评测（手动标注 20+ 场比赛） | 召回率 > 90%，精确率 > 85% | 待完成 |
| 2.7 | 自动识别结果人工修正 | 自动候选片段 → 用户确认/增删改 → Phase 1 配置 | 已完成基础版 |
| 2.8 | 长视频识别后台任务化 | OCR 进度反馈、可取消、避免长请求阻塞 | 待完成 |
| 2.9 | 模板/音频多信号融合 | OCR 之外的补充检测策略 | 待开始 |

### 4.5 Phase 2 成功标准

- [ ] 丢入完整游戏录屏（20-40 分钟），自动输出含所有多杀片段的高光视频
- [ ] 五杀召回率 > 95%、四杀 > 90%（不可漏检）
- [ ] 误检率 < 15%（允许少量误检，可手动剔除）
- [ ] 片段边界合理，覆盖团战全程
- [ ] OCR/模板/音频识别失败时，用户仍可回退到手动标注并完成剪辑

### 4.6 Phase 2 进度记录

| 日期 | 进度 |
|------|------|
| 2026-05-18 | 新增 `src/detector/` 模块骨架，包括事件解析、边界计算、帧采样/ROI 工具和 OCR 适配接口。 |
| 2026-05-18 | 完成中英文多杀文本基础解析：`double_kill`、`triple_kill`、`quadra_kill`、`penta_kill`。 |
| 2026-05-18 | 完成事件去重和置信度过滤，避免同一横幅在连续采样帧中重复生成事件。 |
| 2026-05-18 | 完成事件到 clip 窗口的自动边界计算，支持回溯/前瞻窗口、视频边界裁剪、近邻事件合并。 |
| 2026-05-18 | 明确 Phase 2 必须保留手动标注能力，自动识别结果只作为候选片段，用户可人工修正。 |
| 2026-05-18 | 新增候选片段生成接口 `/api/detector/candidates`，可将文本观测转换为事件和 Phase 1 clips。 |
| 2026-05-18 | Web 新增“自动识别候选”面板，支持粘贴识别文本、生成候选片段，并追加到手动片段列表中继续精修。 |
| 2026-05-18 | 接入 PaddleOCR 后端：新增 `PaddleOcrReader`、视频 OCR pipeline 和 `/api/detector/video`。 |
| 2026-05-18 | 按方案调整：移除旧 OCR 后端，直接以 PaddleOCR 作为默认视频 OCR 引擎，保留手动标注兜底。 |
| 2026-05-18 | 更新 OCR 依赖安装说明：PaddleOCR/PaddlePaddle 纳入 `requirements.txt` 必选依赖。 |
| 2026-05-18 | Web “自动识别候选”面板新增“从当前视频自动识别”，支持 ROI、采样间隔、最多帧数和最小置信度配置。 |
| 2026-05-18 | 当前集成方式为“识别生成候选 → 追加到手动片段列表 → 人工修正 → 运行 Phase 1 导出”，尚未做一键后台 OCR 到导出。 |

### 4.7 计划与当前实现差异

| 原计划/预期 | 当前实现 | 计划调整 |
|-------------|----------|----------|
| Phase 2 仍处于帧采样/OCR 接入阶段 | 已完成帧采样、ROI 裁剪、PaddleOCR 接入、事件解析、候选片段生成和 Web 候选追加 | 将 2.1、2.2、2.5、2.7 调整为“已完成基础版” |
| “丢入录屏即可自动输出成片” | 当前是先识别候选，再由用户确认/修正，最后手动点击运行导出 | 保持半自动闭环，后续再做一键后台 OCR 到导出 |
| OCR 引擎存在多个备选方案 | 已按决策移除旧 OCR 备选，直接使用 PaddleOCR | 文档和依赖统一为 PaddleOCR/PaddlePaddle |
| Web 只规划基础配置器 | 已包含双模式切换、逐段转场、视频 OCR 候选面板 | Web 下一步补 SFX 文件映射和长任务进度 |
| 准确率和质量验收 | 目前只有单元/构建验证，缺真实录屏评测 | 下一阶段优先真实录屏验收和质量指标脚本 |

---

## 五、Phase 3+ — 高级视觉特效（远期规划）

### 5.1 镜头聚焦与动态跟踪

- **英雄跟踪**：基于 YOLO/目标检测跟踪英雄位置
- **动态裁剪**：根据英雄运动轨迹自动调整裁剪窗口
- **关键帧平滑**：避免画面抖动

### 5.2 动态缩放（Ken Burns / Dolly Zoom）

- 击杀瞬间慢放 + 推进缩放
- 团战关键时刻 focus 核心英雄

### 5.3 慢动作特效

- FFmpeg `minterpolate` / `tblend` 滤镜
- 光流法补帧（SVP/RIFE）实现平滑慢放
- 击杀瞬间 0.25x ~ 0.5x 变速

### 5.4 视觉覆盖层

- 击杀图标动画叠加
- 英雄头像蒙版
- 文字弹幕效果
- 粒子特效

### 5.5 智能构图

- 识别画面核心区域自动裁剪为竖版（适配短视频平台）
- 多信息源（小地图、装备栏）智能取舍

---

## 六、当前项目结构

```
PentaForge/
├── LOCAL_SETUP.md            # 本地安装、启动、测试、OCR 可选依赖说明
├── DESIGN.md                 # 阶段规划与进度记录
├── cli.py                    # CLI 入口
├── pentaforge/
│   ├── requirements.txt      # 后端运行依赖（含 PaddleOCR）
│   ├── requirements-dev.txt  # 测试/开发依赖
│   ├── config/
│   │   ├── default.yaml      # 默认配置模板
│   │   └── example.yaml      # 示例配置
│   ├── src/
│   │   ├── __init__.py
│   │   ├── config_parser.py  # 模块 A: 配置解析
│   │   ├── clip_extractor.py # 模块 B: 片段裁剪
│   │   ├── audio_processor.py # 模块 C: 音频处理
│   │   ├── clip_stitcher.py  # 模块 D: 转场拼接
│   │   ├── pipeline.py       # 模块 E: 编排与导出
│   │   ├── server.py         # FastAPI + WebSocket 服务
│   │   └── detector/         # Phase 2 智能检测
│   │       ├── __init__.py
│   │       ├── ocr_reader.py # OCR 适配协议
│   │       ├── paddleocr_reader.py # PaddleOCR 实现
│   │       ├── frame_sampler.py # 视频帧采样与 ROI 裁剪
│   │       ├── event_parser.py # 事件解析
│   │       ├── detect_pipeline.py # 视频 OCR 到候选片段
│   │       └── boundary.py   # 片段边界计算
│   ├── tests/                # 配置、FFmpeg、检测模块测试
│   └── web/                  # Vue 3 + Vite 前端
└── static/                   # Vue build 输出，由后端静态托管
```

---

## 七、风险与注意事项

### 7.1 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| FFmpeg 版本兼容性 | 部分滤镜不可用 | 锁定 FFmpeg >= 5.0，文档注明最低版本 |
| OCR 对中文/英文客户端识别率不稳定 | Phase 2 检测准确率下降 | 多策略融合，允许用户反馈修正 |
| 自动识别漏检/误检 | 用户错过高光或生成无关片段 | 保留手动标注入口，自动结果只作为候选，允许人工增删改 |
| 不同分辨率/UI缩放 | OCR ROI 定位偏移 | 按比例定位而非绝对像素 |
| 极端时长录屏（>1h） | 内存/时间开销大 | 分段处理，关键帧采样 |
| 游戏版本更新 UI 变化 | 模板匹配/OCR 失效 | 模块化检测策略，模板可替换 |

### 7.2 文档维护注意事项

- 每次实现、调整或删除阶段功能时，必须同步更新本文件中的阶段计划、任务状态和进度记录。
- 新增配置字段时，需要同时更新配置示例、Web 表单说明和测试覆盖。
- 如果某项功能只是部分完成，状态写为“进行中”，并在进度记录中说明剩余工作。
- 阶段完成标准不能只看代码是否可运行，还需要记录测试结果和未覆盖风险。

---

## 八、下一步行动

1. 用真实《英雄联盟》录屏验证 PaddleOCR 的识别率、ROI 默认值和采样间隔，并记录召回/误检样本。
2. 为 `/api/detector/video` 增加后台任务、进度反馈和取消能力，避免长视频 OCR 请求阻塞页面。
3. 完善 Web 表单的 SFX 文件映射编辑，目前 Web 只配置 BGM 和 SFX 总音量。
4. 补充 Phase 1 质量验收脚本：PSNR、音画同步、黑帧检测、处理速度统计。
5. 设计模板匹配或音频指纹作为 OCR 漏检时的第二信号源，再做多信号融合。
6. 使用真实录屏素材做“识别候选 → 手动修正 → 导出成片”的端到端验收，并记录测试结果。
