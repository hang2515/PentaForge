-r requirements-dev.txt

# Optional OCR dependencies for the "detect from current video" workflow.
# Verified locally with paddlepaddle 3.3.1 and paddleocr 3.5.0 on Python 3.13.
paddlepaddle>=3.0.0
paddleocr>=3.0.0
